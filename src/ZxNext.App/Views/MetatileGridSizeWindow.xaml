<Window x:Class="ZxNext.App.Views.MetatileGridSizeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Choose Metatile Size" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="16" Width="300">
        <TextBlock TextWrapping="Wrap" FontSize="11" Margin="0,0,0,10"
                   Text="Pick the metatile size for this project — every metatile and every map's grid shares this one size, fixed forever once chosen." />
        <RadioButton x:Name="Size2" Content="2x2" GroupName="GridSize" IsChecked="True" Margin="0,0,0,6" />
        <RadioButton x:Name="Size4" Content="4x4" GroupName="GridSize" Margin="0,0,0,10" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" IsCancel="True" Click="Cancel_OnClick" />
            <Button Content="OK" Width="80" IsDefault="True" Click="Ok_OnClick" />
        </StackPanel>
    </StackPanel>
</Window>
