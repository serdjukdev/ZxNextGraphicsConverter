<Window x:Class="ZxNext.App.Views.UserByteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Set User Byte" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="14" Width="300">
        <TextBlock Text="User byte (0-255), exported as this object's 8th byte:" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,4" />
        <TextBox x:Name="ValueBox" TextChanged="ValueBox_OnTextChanged" />

        <TextBlock Text="Bits (MSB to LSB):" FontSize="11" Margin="0,12,0,4" />
        <UniformGrid Columns="8">
            <ToggleButton x:Name="Bit7" Content="128" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit6" Content="64" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit5" Content="32" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit4" Content="16" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit3" Content="8" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit2" Content="4" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit1" Content="2" Click="BitButton_OnClick" Margin="0,0,2,0" />
            <ToggleButton x:Name="Bit0" Content="1" Click="BitButton_OnClick" />
        </UniformGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" IsCancel="True" Click="Cancel_OnClick" />
            <Button Content="OK" Width="80" IsDefault="True" Click="Ok_OnClick" />
        </StackPanel>
    </StackPanel>
</Window>
