<Window x:Class="ZxNext.App.Views.ScrollableMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Width="480" SizeToContent="Height" MaxHeight="560"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel Margin="16">
        <!-- Coloured accent bar in place of a native MessageBox icon — set from MessageBoxImage in code-behind. -->
        <Border x:Name="AccentBar" Height="3" Margin="0,0,0,12" />

        <!-- Fixed-size scrollable body: what the old plain MessageBox.Show(string.Join("\n", ...)) calls
             this replaces would grow unboundedly tall for a long cascade description (e.g. hundreds of
             affected tile/metatile names) with no way to scroll — this caps it instead. -->
        <ScrollViewer MaxHeight="380" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <TextBlock x:Name="MessageText" FontSize="12" TextWrapping="Wrap" />
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="YesButton" Content="Yes" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Yes_OnClick" />
            <Button x:Name="NoButton" Content="No" Width="80" IsCancel="True" Click="No_OnClick" />
            <Button x:Name="OkButton" Content="OK" Width="80" IsDefault="True" IsCancel="True" Click="Ok_OnClick" />
        </StackPanel>
    </StackPanel>
</Window>
